<%#
 Copyright 2022 the original author or authors from the PyHipster project.

 This file is part of the PyHipster project, see https://github.com/sudeephazra/generator-pyhipster
 for more information.

 Licensed under the Apache License, Version 2.0 (the "License");
 you may not use this file except in compliance with the License.
 You may obtain a copy of the License at

      https://www.apache.org/licenses/LICENSE-2.0

 Unless required by applicable law or agreed to in writing, software
 distributed under the License is distributed on an "AS IS" BASIS,
 WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 See the License for the specific language governing permissions and
 limitations under the License.
-%>
from datetime import datetime
from enum import Enum
from typing import List
<%_ Object.keys(uniqueEnums).forEach(function(element) { _%>
from .enumeration.<%= element %> import <%= element %>
<%_ }); _%>
<%_ for (const otherEntity of otherEntities.filter(otherEntity => otherEntity.entityClass !== persistClass)) { _%>
from . import <%= otherEntity.entityClass %>
<%_ } _%>
from DatabaseConfig import db
<% 
    const javaFieldMapping = {
        "Long": "Integer",
        "Instant": "DateTime",
        "Duration": "Interval",
        "Double": "Float",
        "Blob": "LargeBinary",
        "AnyBlob": "LargeBinary",
        "ImageBlob": "LargeBinary",
        "TextBlob": "UnicodeText"
    }
%> 

class <%= persistClass %>(db.Model):
    __tablename__ = "<%= persistClass %>"
    <%_ if (!embedded && primaryKey) { _%>
    <%= primaryKey.name %> = db.Column(db.<%= javaFieldMapping[primaryKey.type] %>, primary_key=True)
    <%_ } _%>
    <%_ for (const field of fields.filter(field => !field.transient && !field.id && !field.fieldIsEnum)) { _%>
    <%= _.replace(_.lowerCase(field.fieldName), ' ', '_') %> = db.Column(db.<%= javaFieldMapping[field.javaFieldType] || field.javaFieldType %><% if (field.fieldValidationRequired) { %>, nullable=False<% } %><% if (field.fieldValidationUnique) { %>, unique=True<% } %>)
    <%_ } _%>
    <%_ for (const field of fields.filter(field => !field.transient && field.fieldIsEnum)) { _%>
    <%= _.replace(_.lowerCase(field.fieldName), ' ', '_') %> = db.Enum(<%= field.javaFieldType %><% if (field.fieldValidationRequired) { %>, nullable=False<% } %><% if (field.fieldValidationUnique) { %>, unique=True<% } %>)
    <%_ } _%>

    @classmethod
    def find_by_<%= primaryKey.name %>(cls, _<%= primaryKey.name %>) -> "<%= persistClass %>":
        return cls.query.filter_by(<%= primaryKey.name %>=_<%= primaryKey.name %>).first()

    @classmethod
    def find_all(cls) -> List["<%= persistClass %>"]:
        return cls.query.all()

    @classmethod
    def find_all_count(cls):
        return cls.query.count()
    
    def save_to_db(self) -> None:
        db.session.add(self)
        db.session.commit()

    def delete_from_db(self) -> None:
        db.session.delete(self)
        db.session.commit()

    # FIXME: Add relationships


    # Getters and setters
    <%_ for (const field of fields.filter(field => !field.transient && (embedded || !field.id || !primaryKey.composite))) { _%>
    def get_<%= _.replace(_.lowerCase(field.fieldInJavaBeanMethod), ' ', '_') %>(self):
        return self.<%= _.replace(_.lowerCase(field.fieldName), ' ', '_') %>

    def set_<%= _.replace(_.lowerCase(field.fieldInJavaBeanMethod), ' ', '_') %>(self, <%= _.replace(_.lowerCase(field.fieldName), ' ', '_') %>):
        self.<%= _.replace(_.lowerCase(field.fieldName), ' ', '_') %> = <%= _.replace(_.lowerCase(field.fieldName), ' ', '_') %>
    
    <%_ } _%>
    # __repr__ and __str__ methods
    def __repr__(self):
        return f"<%= persistClass %>(" + \
        <%_ if (!embedded) { _%>
                    "<%= primaryKey.name %>=" + {self.<%= primaryKey.name %>} + \
        <%_ } _%>
        <%_ for (const field of fields.filter(field => !field.id && !field.transient)) {
        const isNumeric = field.fieldTypeNumeric; _%>
                    ", <%= _.replace(_.lowerCase(field.fieldName), ' ', '_') %>=<% if (! isNumeric) { %>'<% } %>" + <% if ( !field.fieldTypeString ) { %>{self.<%= _.replace(_.lowerCase(field.fieldName), ' ', '_') %>}<% } else { %>{self.<%= _.replace(_.lowerCase(field.fieldName), ' ', '_') %>}<% } %><% if (! isNumeric) { %> + "'" <% } %> + \
        <%_ if (field.fieldWithContentType) { _%>
                    ", <%= _.replace(_.lowerCase(field.fieldName), ' ', '_') %>ContentType='" + self.<%= _.replace(_.lowerCase(field.fieldName), ' ', '_') %>ContentType + "'" + \
        <%_ } _%>
        <%_ } _%>
        <%_ for (const relationship of relationships) { _%>
        <%_ if (relationship.otherEntity.embedded) {
            if (relationship.relationshipManyToOne) { _%>
                    ", <%= relationship.relationshipFieldName %>='" + get<%= relationship.relationshipNameCapitalized %>() + "'" + \
            <%_ } else if (relationship.relationshipOneToOne && relationship.ownerSide) { _%>
                    ", <%= relationship.relationshipFieldName %>='" + get<%= relationship.relationshipNameCapitalized %>() + "'" + \
            <%_ } _%>
        <%_ } _%>
        <%_ } _%>
                    ")" 

    def __str__(self):
        return f"<%= persistClass %>(" + \
        <%_ if (!embedded) { _%>
                    "<%= primaryKey.name %>=" + str(self.<%= primaryKey.name %>) + \
        <%_ } _%>
        <%_ for (const field of fields.filter(field => !field.id && !field.transient)) {
        const isNumeric = field.fieldTypeNumeric; _%>
                    ", <%= _.replace(_.lowerCase(field.fieldName), ' ', '_') %>=<% if (! isNumeric) { %>'<% } %>" + <% if ( !field.fieldTypeString ) { %>str(self.<%= _.replace(_.lowerCase(field.fieldName), ' ', '_') %>)<% } else { %>self.<%= _.replace(_.lowerCase(field.fieldName), ' ', '_') %><% } %><% if (! isNumeric) { %> + "'" <% } %> + \
        <%_ if (field.fieldWithContentType) { _%>
                    ", <%= _.replace(_.lowerCase(field.fieldName), ' ', '_') %>ContentType='" + self.<%= _.replace(_.lowerCase(field.fieldName), ' ', '_') %>ContentType + "'" + \
        <%_ } _%>
        <%_ } _%>
        <%_ for (const relationship of relationships) { _%>
        <%_ if (relationship.otherEntity.embedded) {
            if (relationship.relationshipManyToOne) { _%>
                    ", <%= relationship.relationshipFieldName %>='" + get<%= relationship.relationshipNameCapitalized %>() + "'" + \
            <%_ } else if (relationship.relationshipOneToOne && relationship.ownerSide) { _%>
                    ", <%= relationship.relationshipFieldName %>='" + get<%= relationship.relationshipNameCapitalized %>() + "'" + \
            <%_ } _%>
        <%_ } _%>
        <%_ } _%>
                    ")" 
